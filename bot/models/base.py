from sqlalchemy.ext.declarative import declarative_base, DeclarativeMeta

# Base: DeclarativeMeta = declarative_base() # Commented out to prevent duplicate Base definition
# Ensure all models use Base from bot.database.base
